services:

  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: email_signatures
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  api:

    build: .

    ports:

      - "8000:8000"

    environment:

      APP_ENV: development

      APP_DEBUG: "true"

      # DATABASE_URL: "postgresql+psycopg://postgres:postgres@db:5432/email_signatures"

    depends_on:
      db:
        condition: service_healthy

    volumes:

      - ./app:/app/app

volumes:
  postgres_data:
